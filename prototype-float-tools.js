(function () {
  "use strict";

  if (window.__prototypeFloatToolsLoaded) return;
  window.__prototypeFloatToolsLoaded = true;

  var currentScript = document.currentScript;
  var cssHref = currentScript && currentScript.src
    ? currentScript.src.replace(/\.js(?:\?.*)?$/, ".css")
    : new URL("../prototype-float-tools.css", location.href).href;
  if (!document.querySelector('link[data-prototype-tools="style"]')) {
    var styleLink = document.createElement("link");
    styleLink.rel = "stylesheet";
    styleLink.href = cssHref;
    styleLink.dataset.prototypeTools = "style";
    document.head.appendChild(styleLink);
  }

  function menu(group, pages) {
    return { group: group, pages: pages };
  }

  function note(core, data, rules) {
    return { core: core, data: data, rules: rules || "" };
  }

  function pages(list) {
    return list.map(function (name) { return page(name); });
  }

  // CloudBase static hosting stores Chinese filenames as #Uxxxx object keys.
  // Keep ordinary filenames for local preview and GitHub Pages.
  function cloudPath(file) {
    if (!/tcloudbaseapp\.com$/i.test(location.hostname)) return file;
    return String(file).replace(/[^\x00-\x7F]/g, function (character) {
      return "%23U" + character.charCodeAt(0).toString(16);
    });
  }

  function page(name, children) {
    return { name: name, file: cloudPath(name + ".html"), children: children || [] };
  }

  function route(name, file, children, matchCurrent, defaultCurrent) {
    return {
      name: name,
      file: cloudPath(file),
      children: children || [],
      matchCurrent: matchCurrent !== false,
      defaultCurrent: defaultCurrent === true
    };
  }

  var CONFIGS = [
    {
      match: "_PC端HTML原型_V2",
      publishMatch: "/pc/",
      endpoint: "PC端",
      menus: [
        menu("专区首页", [page("首页")]),
        menu("培训课程", [
          page("培训课程", [
            page("课程详情", [
              page("个人培训报名", [
                route("个人报名资料", "个人报名资料.html", [], false),
                page("培训报名详情")
              ]),
              route("发起团队报名", "团队报名任务编辑.html", [
                route("团队报名详情", "团队报名任务详情.html", [], false)
              ]),
              page("培训机构详情")
            ])
          ])
        ]),
        menu("考试服务", [
          page("考试服务", [page("评价项目详情")])
        ]),
        menu("我的培训考试", [
          route("业务总览（个人）", "我的培训考试.html?role=personal#overview", [
            route("报名订单支付", "报名订单支付.html", [], false),
            page("我的培训", [page("在线课程学习"), page("线下培训日程签到"), page("培训过程完成结果")]),
            page("考试安排"),
            page("成绩查询复核"),
            route("证书与档案", "证书与档案.html", [], false)
          ], true, true),
          route("我的订单（个人）", "我的培训考试.html?role=personal#orders", [
            route("个人报名订单支付", "报名订单支付.html", [page("报名支付成功")]),
            route("报名订单详情", "报名订单详情.html?order=REG2026081300128")
          ]),
          route("我的订单（团队负责人）", "我的培训考试.html?role=team#orders", [
            route("团队报名缴费", "团队报名订单支付.html", [
              route("团队订单详情", "报名订单详情.html?order=REG-TEAM-20260813001")
            ])
          ]),
          route("个人资料（个人）", "我的培训考试.html?role=personal#profile", [page("个人报名资料")]),
          route("我的证书（个人）", "我的培训考试.html?role=personal#certificates", [
            route("证书详情", "证书与档案.html")
          ]),
          route("团队报名（团队负责人）", "我的培训考试.html?role=team#teams", [
            route("团队报名详情", "团队报名任务详情.html")
          ])
        ]),
        menu("证书查验", [route("证书真伪查验", "证书真伪查询.html")])
      ],
      notes: {
        "首页.html": note("汇集选课、报名、培训、考试、成绩与证书入口，承接用户进入一期培训考试取证主链路。", "Banner、公告和政策由平台运营端发布；课程班次由培训机构维护；考试、成绩和证书由评价机构产生；个人进度按登录用户业务记录汇总。", "公开内容可直接查看，报名、学习、成绩和证书等个人业务需登录后访问。"),
        "培训课程.html": note("汇集当前可报名培训课程，帮助用户选择工种、课程和对应培训班次。", "课程及班次由培训机构维护，发布状态、报名期限和剩余名额由课程及班次业务更新。", "仅已发布且存在有效报名班次的课程进入可报名范围。"),
        "培训机构.html": note("汇集可提供产业工人培训服务的机构及其有效课程。", "机构资料由平台运营端维护，课程与班次由对应培训机构发布并随服务状态更新。", "仅服务状态正常且存在有效课程的培训机构对外展示。"),
        "考试服务.html": note("汇集可参与的评价项目及考试服务信息，承接培训完成后的考试查询。", "评价项目、排考和考试安排由评价机构产生，个人可见数据按报名及送考记录关联。"),
        "证书真伪查询.html": note("核验职业技能证书是否为平台有效发放记录。", "查验数据来源于评价机构完成发放并同步至平台的证书档案。", "证书编号由系统生成并唯一标识一份证书；仅已发放且未失效的记录返回有效结果。"),
        "课程详情.html": note("集中查看课程、可报名班次、培训机构和费用，确认后发起培训报名。", "课程及班次由培训机构维护，费用、报名期限和名额随对应班次数据更新。", "仅处于报名期且有剩余名额的有效班次可提交报名。"),
        "培训机构详情.html": note("集中查看培训机构资质、服务信息及当前可报名课程。", "机构资料及服务状态由平台运营端维护，课程数据由该培训机构发布。", "仅服务状态正常的机构展示当前有效可报名课程。"),
        "个人报名资料.html": note("维护个人报名和后续考试复用的唯一人员基础档案。", "实名信息由本人提交并沿用认证结果，联系、从业和材料信息由本人维护。", "同一身份仅保留一份人员主档；已认证身份字段按现有认证结果使用。"),
        "个人培训报名.html": note("确认班次和本人资料，形成一笔个人培训报名并提交培训机构受理。", "班次来自课程详情，个人信息沿用人员主档，本次补充材料由报名人提交。", "报名编号由系统提交报名时自动生成，用于唯一标识一笔报名；同一人员不得重复提交同一有效班次。"),
        "培训报名详情.html": note("集中查看一笔培训报名的资料、费用和当前办理结果。", "报名资料由个人或团队提交，受理结果由培训机构更新，缴费和培训状态由后续业务环节回写。", "报名编号由系统自动生成并唯一标识该笔报名，后续订单、培训和考试记录沿用关联。"),
        "团队报名任务.html": note("管理团队报名及成员资料，形成可提交培训机构受理的团队报名记录。", "团队信息和成员资料由团队负责人维护，课程班次来源于当前有效招生数据。", "团队报名编号由系统自动生成并唯一标识一次团队报名，成员分别关联个人报名记录。"),
        "团队报名任务编辑.html": note("维护团队及成员报名资料，完成团队报名提交或退回后的补充。", "原团队信息和成员资料来自团队报名记录，新增或补充内容由团队负责人维护。", "仅草稿或待补充状态可修改；已提交受理的成员不得重复加入同一有效报名。"),
        "团队报名任务详情.html": note("集中查看一次团队报名的成员、费用及受理和缴费结果。", "团队资料由负责人提交，成员受理结果由培训机构产生，订单状态由支付或核款环节更新。", "团队报名编号由系统自动生成并唯一标识本次团队报名。"),
        "报名订单支付.html": note("完成个人报名订单缴费，使已受理报名进入后续培训环节。", "订单由培训机构受理报名后生成，金额来源于报名课程费用，支付结果由支付或核款环节更新。", "订单编号由系统生成并唯一对应一笔应付业务；仅有效待支付订单可办理缴费。"),
        "团队报名订单支付.html": note("完成团队报名订单缴费，并提交线下付款所需的付款信息和凭证。", "团队订单由成员受理结果汇总生成，金额按受理成员及课程费用计算，核款结果由培训机构更新。", "订单编号由系统自动生成并唯一标识该笔团队应付；已支付订单不得重复缴费。"),
        "报名订单详情.html": note("集中查看一笔报名订单的费用构成、付款信息和关联报名。", "订单由报名受理结果产生，支付信息来自在线支付或线下凭证，状态由支付或核款结果更新。", "订单编号由系统自动生成并唯一标识该笔订单。"),
        "我的订单.html": note("汇总当前登录人关联的个人和团队报名订单及缴费结果。", "订单由培训机构受理报名后产生，金额来自对应报名，状态由支付或核款环节更新。", "每个订单编号唯一对应一笔应付业务。"),
        "我的培训考试.html": note("汇总当前用户在报名、缴费、培训、考试、成绩和证书环节的办理进度。", "各环节数据分别由报名人、培训机构和评价机构产生，并按人员身份及平台报名编号关联。", "个人仅查看本人记录；团队负责人同时查看本人负责的团队报名。"),
        "我的培训.html": note("汇总已生效培训任务及当前学习进度，承接线上学习和线下培训。", "培训任务由报名受理并完成缴费后产生，进度由在线学习、签到和实训记录累计更新。", "仅已入班且处于有效培训期的任务可继续学习；任务沿用对应平台报名编号。"),
        "在线课程学习.html": note("完成班次内在线课程学习并沉淀章节进度和学习时长。", "课程内容和学习要求由培训机构配置，学习记录由当前登录学员的完成行为产生。", "同一章节完成结果不重复累计，学习记录沿用对应平台报名编号。"),
        "线下培训日程签到.html": note("记录学员参加线下培训课次的出勤结果，作为培训完成依据。", "课次日程由培训机构发布，签到记录由学员在对应课次产生并同步培训进度。", "仅在有效签到时段内记录出勤，同一学员同一课次不重复计数。"),
        "评价项目详情.html": note("查看评价项目的适用范围和考试要求，为后续机构送考提供依据。", "项目内容由评价机构维护，项目状态和有效期随评价机构业务配置更新。", "送考由培训机构在学员完成培训后发起，个人不在本页直接创建送考。"),
        "考试安排.html": note("集中查看本人已排定考试的时间、地点和准考信息。", "考试安排由评价机构完成排考后产生，并按送考记录及平台报名编号同步至考生。", "仅已通过送考审核并完成排考的考生形成考试安排。"),
        "成绩查询复核.html": note("查看已发布考试成绩，并在允许范围内提交成绩复核。", "成绩由考试执行记录形成并由评价机构发布，复核状态和结论由评价机构处理后更新。", "仅已发布成绩可查询；复核申请不直接修改原成绩。"),
        "证书与档案.html": note("查看本人已取得证书及其关联的培训、考试过程档案。", "培训记录由培训机构产生，考试成绩和证书由评价机构产生，平台按报名编号汇总。", "证书编号由系统生成并唯一标识一份证书；仅符合发证条件并已发放的证书对本人展示。"),
        "报名支付成功.html": note("确认一笔报名订单已完成支付，并承接进入后续培训业务。", "支付结果来自本次订单支付，成功状态同步回写订单及关联报名。", "支付结果页不重复生成订单；订单编号继续关联原报名和培训任务。"),
        "培训过程完成结果.html": note("确认当前培训任务已达到完成要求，形成后续机构送考的数据基础。", "完成结果由线上学时、线下出勤和实训记录汇总，并由培训机构确认。", "各项培训要求均达标后方可转为培训完成；结果沿用对应平台报名编号。")
      }
    },
    {
      match: "_平台运营管理端HTML原型_V2",
      publishMatch: "/platform/",
      endpoint: "平台运营管理端",
      menus: [
        menu("专区内容管理", [
          page("Banner管理", [page("Banner详情"), page("编辑Banner")]),
          page("通知公告管理", [page("通知公告详情"), page("编辑通知公告")]),
          page("政策管理", [
            route("政策资讯", "政策管理.html?tab=policy", [
              route("政策资讯详情", "政策详情.html"),
              route("新增/编辑政策资讯", "编辑政策.html")
            ], true, true),
            route("政策补贴", "政策管理.html?tab=subsidy", [
              route("政策补贴详情", "政策补贴详情.html"),
              route("新增/编辑政策补贴", "编辑政策补贴.html")
            ])
          ])
        ]),
        menu("培训课程管理", [
          page("课程管理", [page("课程详情")]),
          page("班次管理", [page("班次详情")])
        ]),
        menu("考试项目管理", [
          page("考试管理", [page("考试详情")])
        ]),
        menu("机构管理", [
          page("培训机构管理", [page("培训机构详情"), page("编辑培训机构")]),
          page("评价机构管理", [page("评价机构详情"), page("编辑评价机构")])
        ]),
        menu("报名管理", [
          page("报名记录", [page("报名记录详情")]),
          page("报名订单", [page("报名订单详情")])
        ]),
        menu("送考管理", [page("送考进度", [page("送考进度详情")])]),
        menu("业务档案", [
          page("一人一档", [page("一人一档详情")]),
          page("培训档案", [page("培训档案详情")]),
          page("考试档案", [page("考试档案详情")]),
          page("证书档案", [page("证书档案详情")])
        ])
      ],
      notes: {
        "工作台.html": note("汇总平台一期业务运行概况，为运营人员识别待处理事项提供参考。", "统计来源于机构、报名、培训、送考、考试和证书等业务环节的当前记录。", "工作台一期不作为独立业务办理入口，具体业务由对应菜单承接。"),
        "Banner管理.html": note("管理专区首页轮播内容及其展示状态。", "Banner由平台运营人员创建维护，前台展示结果随终端、有效期和发布状态更新。", "Banner编号由系统创建内容时自动生成并唯一标识一条记录；仅有效且展示中的内容进入对应前台。"),
        "Banner详情.html": note("集中查看一条Banner内容及其当前发布配置。", "数据来源于平台运营人员保存的Banner记录，展示状态由发布操作和有效期共同更新。", "Banner编号由系统自动生成并唯一标识该记录。"),
        "编辑Banner.html": note("新增或维护Banner内容，形成专区首页可发布的轮播记录。", "名称、终端、位置、跳转内容和图片由平台运营人员维护。", "展示终端仅选择PC端或H5端；发布后按有效期和状态进入对应首页。"),
        "通知公告管理.html": note("管理培训考试专区的通知公告及发布状态。", "公告由平台运营人员创建维护，前台可见状态由发布状态和有效期更新。", "公告编号由系统创建时自动生成并唯一标识一条公告；仅已发布且有效的公告对外展示。"),
        "通知公告详情.html": note("集中查看一条通知公告的正文、附件和发布结果。", "公告内容来源于平台运营人员保存的记录，状态由草稿、发布和有效期变化更新。", "公告编号由系统自动生成并唯一标识该公告。"),
        "编辑通知公告.html": note("新增或维护专区通知公告，并完成草稿保存或正式发布。", "公告正文、附件和有效期由平台运营人员录入维护。", "草稿不对外展示；发布后按有效期进入专区公告内容。"),
        "政策管理.html": note("管理培训考试相关政策资讯和政策补贴内容。", "政策由平台运营人员创建维护，展示状态随发布时间、有效期和发布状态更新。", "仅已发布且有效的政策进入专区前台；一期政策补贴适用工种统一为全部工种。"),
        "政策详情.html": note("集中查看一条政策资讯的正文、附件及发布信息。", "内容来源于平台运营端保存的政策记录，当前状态由发布和有效期变化更新。", "政策编号由系统创建时自动生成并唯一标识该政策记录。"),
        "编辑政策.html": note("新增或维护政策资讯，形成专区可发布的政策内容。", "政策正文、发布单位、适用地区、有效期和附件由平台运营人员维护。", "草稿不对外展示，发布后按有效期进入政策资讯。"),
        "政策补贴详情.html": note("集中查看一条政策补贴的适用对象、补贴标准和申报条件。", "补贴内容由平台运营人员维护，状态由申报期限、有效期和发布结果更新。", "补贴政策编号由系统自动生成并唯一标识该记录；一期适用工种统一为全部工种。"),
        "编辑政策补贴.html": note("新增或维护政策补贴内容，形成专区可发布的补贴政策。", "发布单位、适用地区、补贴标准、期限和说明由平台运营人员录入。", "适用工种仅保留全部工种；发布后按有效期对外展示。"),
        "课程发布审核.html": note("处理培训机构提交的课程发布申请，形成课程发布结论。", "课程资料由培训机构提交，审核意见和结果由平台运营人员产生并回写课程状态。", "课程编号由系统创建课程时自动生成；仅待审核课程可办理。"),
        "课程发布审核详情.html": note("集中核对一次课程发布申请并记录审核结论。", "课程及附件来源于培训机构提交，审核意见由平台运营人员录入。", "课程编号唯一标识当前申请；审核结果提交后更新课程状态。"),
        "课程管理.html": note("汇总平台培训课程及其发布和招生状态，支撑课程运营监管。", "课程由培训机构维护，发布状态和班次招生情况由对应业务环节更新。", "课程编号由系统创建课程时自动生成，用于唯一标识一门课程。"),
        "课程详情.html": note("集中查看一门课程的配置、所属机构和关联班次情况。", "课程资料由培训机构维护，班次及报名数据由后续招生和培训业务产生。", "课程编号由系统自动生成并唯一标识该课程。"),
        "班次管理.html": note("汇总各培训机构开设的培训班次及实施状态。", "班次由培训机构基于课程创建，人数和状态由报名、开班和培训完成环节更新。", "班次编号由系统创建班次时自动生成，用于唯一标识一次培训班次。"),
        "班次详情.html": note("集中查看一次培训班次的学员和实施结果。", "班次及日程由培训机构维护，学员来自已生效报名，学时、出勤和实训数据由培训过程产生。", "班次编号由系统自动生成并唯一标识该班次。"),
        "考试项目审核.html": note("处理评价机构提交的考试项目申请，形成项目发布结论。", "项目资料由评价机构提交，审核意见和结果由平台运营人员产生并回写项目状态。", "考试项目编号由系统创建项目时自动生成；仅待审核项目可办理。"),
        "考试项目审核详情.html": note("集中核对一次考试项目申请并记录审核结论。", "项目及附件来源于评价机构提交，审核意见由平台运营人员录入。", "考试项目编号唯一标识当前申请；审核结果提交后更新项目状态。"),
        "考试管理.html": note("汇总平台考试项目及其有效状态，支撑送考和考务业务监管。", "考试项目由评价机构维护，状态和关联批次由项目发布、送考及排考业务更新。", "考试项目编号由系统创建项目时自动生成，用于唯一标识一个考试项目。"),
        "考试详情.html": note("集中查看一个考试项目的要求、所属评价机构和关联业务。", "项目资料由评价机构维护，送考批次和考试安排由后续送考、排考环节产生。", "考试项目编号由系统自动生成并唯一标识该项目。"),
        "培训机构管理.html": note("管理培训机构主体、管理员及服务状态，控制其一期业务权限。", "机构及管理员信息由平台运营人员维护，资质和服务状态随机构维护操作更新。", "机构编号由系统新增机构时自动生成并唯一标识一个培训机构。"),
        "培训机构详情.html": note("集中查看一个培训机构的主体、管理员、资质和关联业务。", "机构及管理员信息由平台运营端维护，课程、班次和报名数据由该机构业务产生。", "机构编号由系统自动生成并唯一标识该培训机构。"),
        "编辑培训机构.html": note("新增或维护培训机构主体、管理员、资质和服务信息。", "机构资料和管理员信息由平台运营人员录入，关联业务沿用已保存的机构记录。", "新增时由系统生成唯一机构编号；停用机构不再承接新的课程和报名业务。"),
        "评价机构管理.html": note("管理评价机构主体、管理员及服务状态，控制其一期考试业务权限。", "机构及管理员信息由平台运营人员维护，资质和服务状态随机构维护操作更新。", "机构编号由系统新增机构时自动生成并唯一标识一个评价机构。"),
        "评价机构详情.html": note("集中查看一个评价机构的主体、管理员、资质和关联业务。", "机构及管理员信息由平台运营端维护，考试项目、送考审核和证书数据由该机构业务产生。", "机构编号由系统自动生成并唯一标识该评价机构。"),
        "编辑评价机构.html": note("新增或维护评价机构主体、管理员、资质和服务信息。", "机构资料和管理员信息由平台运营人员录入，关联业务沿用已保存的机构记录。", "新增时由系统生成唯一机构编号；停用机构不再承接新的考试项目和送考业务。"),
        "报名记录.html": note("汇总平台个人及团队成员报名记录，跟踪报名主链路状态。", "报名由个人或团队负责人提交，受理、缴费、培训和送考状态由对应业务角色及环节更新。", "报名编号由系统提交报名时自动生成，用于唯一标识一笔报名。"),
        "报名记录详情.html": note("集中查看一笔报名从提交到后续培训、送考的完整业务记录。", "报名资料由报名人提交，受理和培训由培训机构更新，考试相关状态由评价机构回写。", "报名编号由系统自动生成并贯穿订单、培训、送考和考试关联。"),
        "报名订单.html": note("汇总平台报名订单及其支付、核款和收款状态。", "订单由培训机构受理报名后产生，费用来自报名课程，支付状态由在线支付或核款结果更新。", "订单编号由系统生成并唯一对应一笔应付业务。"),
        "报名订单详情.html": note("集中查看一笔报名订单的费用、付款凭证和关联报名。", "订单来源于报名受理结果，支付信息来自在线支付或线下凭证，状态由支付或核款环节更新。", "订单编号由系统自动生成并唯一标识该笔订单。"),
        "送考批次台账.html": note("汇总全平台送考批次及当前流转结果，用于运营监管。", "批次由培训机构创建，审核结果由评价机构产生，排考状态由评价机构考务环节更新。", "送考批次编号由系统创建批次时自动生成，用于唯一标识一次送考业务；平台端只读监管。"),
        "送考批次详情.html": note("集中查看一次送考业务的名单、审核和排考流转结果。", "批次及考生由培训机构提交，审核和排考结果由评价机构产生。", "送考批次编号由系统自动生成并唯一标识该批次；平台端不修改机构业务结论。"),
        "送考进度.html": note("汇总送考业务在提交、审核、补充和排考环节的办理进度。", "送考批次由培训机构产生，审核及排考节点由评价机构办理结果更新。", "送考批次编号由系统自动生成并唯一标识一次送考业务。"),
        "送考进度详情.html": note("集中查看指定送考批次的跨机构办理进度和当前结果。", "进度由培训机构提交、评价机构审核及排考等业务动作连续更新。", "平台端仅追踪流转结果，不代替培训机构或评价机构办理。"),
        "一人一档.html": note("按人员汇总培训考试取证全过程记录，用于跨业务追溯。", "人员基础信息来自报名资料，培训、送考、考试和证书数据由对应业务环节产生。", "同一人员按身份标识归集，其各次业务通过平台报名编号区分。"),
        "一人一档详情.html": note("集中追溯一个人员历次报名、培训、考试和证书记录。", "档案由各业务端已形成的有效记录汇总，不在档案页重新产生业务数据。", "每次业务沿用对应平台报名编号，人员主档不因重复报名而重复创建。"),
        "培训档案.html": note("汇总已沉淀的培训过程档案，用于追溯培训实施和完成结果。", "报名及班次来自报名业务，学习、签到、实训和完成结论由培训机构产生。", "培训档案按人员及平台报名编号关联一次培训业务。"),
        "培训档案详情.html": note("集中查看一次培训的班次、过程记录和最终完成结果。", "课程班次来自培训管理，学时、出勤、实训及完成结论来自培训机构实施记录。", "档案为已发生业务结果汇总，不在本页修改原始培训记录。"),
        "考试档案.html": note("汇总已沉淀的送考和考试过程档案，用于追溯考试执行结果。", "送考由培训机构产生，排考、准考证、考试结果和成绩由评价机构产生。", "考试档案按人员、平台报名编号及考试批次关联。"),
        "考试档案详情.html": note("集中查看一次考试从送考、排考到成绩形成的完整记录。", "送考资料来源于培训机构，考试安排、执行记录和成绩来源于评价机构。", "档案只汇总已发生业务结果，不在本页修改考试数据。"),
        "证书档案.html": note("汇总平台已生成或已发放证书，用于证书追溯和监管。", "证书由评价机构基于已发布合格成绩生成并发放，状态随发放业务更新。", "证书编号由系统生成并唯一标识一份证书。"),
        "证书档案详情.html": note("集中查看一份证书及其关联人员、考试成绩和发放记录。", "人员信息来自报名主档，成绩和证书信息由评价机构产生并同步平台。", "证书编号由系统自动生成并唯一标识该证书；档案不修改原发放结果。")
      }
    },
    {
      match: "_培训机构运营端HTML原型_V2",
      publishMatch: "/training/",
      endpoint: "培训机构管理端",
      menus: [
        menu("机构信息", [page("机构信息", [page("编辑机构信息")])]),
        menu("课程管理", [page("课程管理", [page("课程详情"), page("编辑课程")])]),
        menu("报名管理", [
          page("个人报名审核", [page("培训报名详情")]),
          page("团队报名审核", [
            route("团队报名审核详情", "培训报名详情.html?source=team&status=pending")
          ]),
          page("报名成员名单", [
            route("报名成员名单详情", "培训报名详情.html?source=members&status=accepted")
          ])
        ]),
        menu("订单管理", [page("报名订单", [page("个人订单详情"), page("报名订单详情")])]),
        menu("培训实施", [
          page("班次管理", [page("班次详情"), page("编辑班次"), page("线下培训过程录入")]),
          page("学员档案", [page("学员档案详情")])
        ]),
        menu("送考管理", [
          page("培训完成名单", [page("培训完成详情")]),
          page("送考批次台账", [page("创建送考批次"), page("送考批次详情")]),
          page("考试安排查询", [page("考试安排详情")])
        ])
      ],
      notes: {
        "机构信息.html": note("集中查看本机构主体、资质、管理员和收款信息。", "机构基础资料由平台运营端维护，本机构补充的服务及收款信息随机构维护结果更新。", "机构编号由系统生成并唯一标识本培训机构。"),
        "编辑机构信息.html": note("维护本机构可自行更新的服务、资质和收款信息。", "原数据来自机构主档，本次变更由培训机构管理员录入并保存。", "机构主体编号保持不变，已认证或平台维护字段按现有权限只读。"),
        "课程管理.html": note("管理本机构培训课程及其招生状态，为班次创建和报名提供课程基础。", "课程由本机构创建维护，报名和班次数量由后续招生及培训业务更新。", "课程编号由系统创建课程时自动生成，用于唯一标识一门课程。"),
        "课程详情.html": note("集中查看一门课程的配置、招生状态和关联班次。", "课程资料由本机构维护，班次和报名数据由后续业务产生。", "课程编号由系统自动生成并唯一标识该课程。"),
        "编辑课程.html": note("新增或维护培训课程，形成后续班次和报名使用的课程记录。", "课程名称、工种、培训方式、学时、费用和材料由本机构录入维护。", "新增时由系统生成唯一课程编号；已被有效班次使用的关键课程信息按状态限制修改。"),
        "个人报名审核.html": note("受理提交至本机构的个人报名，形成通过或退回结果。", "报名资料由个人提交，课程班次来自本机构，受理意见和结果由本机构管理员产生。", "报名编号由系统提交报名时自动生成；受理通过后生成对应报名订单。"),
        "团队报名审核.html": note("受理提交至本机构的团队报名，确认本次团队可接收的成员范围。", "团队及成员资料由团队负责人提交，课程班次来自本机构，受理结果由本机构管理员产生。", "团队报名按一次业务整体受理；退回成员补齐材料后再进入后续处理。"),
        "报名成员名单.html": note("汇总已提交本机构的报名成员及其受理结果，为订单和入班提供人员依据。", "成员来自个人报名和团队报名，基础资料沿用人员主档，受理结果由本机构更新。", "每名成员以系统生成的平台报名编号唯一标识一笔报名。"),
        "培训报名详情.html": note("集中查看一笔个人或团队成员报名资料，并完成受理或查看后续结果。", "报名资料由个人或团队负责人提交，受理结论由本机构产生，缴费和培训状态由后续环节更新。", "报名编号由系统自动生成并唯一标识该笔报名；仅待受理或退回补充状态允许继续办理。"),
        "报名订单.html": note("管理本机构报名订单及收款状态，衔接受理通过后的缴费业务。", "订单由报名受理结果产生，金额来自课程费用，状态由在线支付或本机构核款结果更新。", "订单编号由系统生成并唯一对应一笔应付业务。"),
        "报名订单详情.html": note("集中查看一笔团队或汇总订单的成员、费用和付款凭证。", "订单由受理成员汇总产生，付款信息由团队负责人提交，核款结果由本机构更新。", "订单编号由系统自动生成并唯一标识该笔订单；已支付订单不得重复核款。"),
        "个人订单详情.html": note("集中查看一笔个人报名订单及其支付和关联报名结果。", "订单由个人报名受理通过后产生，支付信息来自用户付款，状态由支付或核款结果更新。", "订单编号由系统自动生成并唯一标识该笔个人订单。"),
        "班次管理.html": note("管理本机构培训班次及其招生、开班和完成状态。", "班次基于本机构课程创建，学员来自已生效报名，状态由开班和培训实施结果更新。", "班次编号由系统创建班次时自动生成，用于唯一标识一次培训班次。"),
        "班次详情.html": note("集中查看一次培训班次的学员和培训实施进度。", "班次日程由本机构维护，学员来自已生效报名，学时、出勤和实训记录由培训过程产生。", "班次编号由系统自动生成并唯一标识该班次。"),
        "编辑班次.html": note("新增或维护培训班次，确定培训时间、日程和实施安排。", "课程来自本机构有效课程，班次日程和教学安排由本机构管理员录入。", "新增时由系统生成唯一班次编号；开班后关键课程和时间信息按状态限制修改。"),
        "线下培训过程录入.html": note("记录班次线下课次、出勤和实训结果，形成培训完成判定依据。", "班次及学员来自培训管理，签到和实训结果由本机构依据实际实施情况录入。", "同一学员同一课次只保留一条有效记录，并按平台报名编号归入培训档案。"),
        "学员档案.html": note("汇总本机构学员的报名和培训过程记录，用于培训业务追溯。", "人员资料来自报名，班次、学习、签到、实训及完成结果来自本机构培训实施。", "档案按人员身份及平台报名编号区分各次培训业务。"),
        "学员档案详情.html": note("集中查看一名学员一次培训的报名、班次和全过程记录。", "报名资料由学员或团队负责人提交，培训过程和完成结果由本机构产生。", "档案沿用平台报名编号汇总已发生记录，不在本页修改原始培训数据。"),
        "培训完成名单.html": note("汇总已达到培训要求的学员，为创建送考批次提供人员范围。", "完成结果由线上学时、线下出勤和实训记录汇总，并由本机构确认。", "名单沿用平台报名编号；仅全部达标且未进入有效送考批次的学员进入。"),
        "培训完成详情.html": note("集中查看一名学员本次培训完成的过程依据和确认结果。", "课程班次来自培训管理，学时、出勤、实训及附件来自本机构实施记录。", "结果沿用平台报名编号，确认后作为送考人员选择依据。"),
        "创建送考批次.html": note("从培训完成名单中形成送考名单，并提交评价机构审核。", "工种、班次和学员来自培训管理，评价项目来自评价机构有效项目，本次送考附件由本机构维护。", "仅培训完成且未进入其他有效送考批次的学员可选择；送考批次编号由系统创建时自动生成并唯一标识本次送考。"),
        "送考批次台账.html": note("管理本机构创建的送考批次，并跟踪审核和排考结果。", "批次及名单由本机构创建，审核状态由评价机构更新，排考结果由评价机构考务环节产生。", "送考批次编号由系统自动生成并唯一标识一次送考业务。"),
        "送考批次详情.html": note("集中查看一次送考业务的名单、提交信息和评价机构审核结果，并承接退回补充。", "批次及考生由本机构提交，审核结果由评价机构产生，补充材料由本机构维护。", "仅待补充状态允许更新退回项并重新提交；批次编号由系统自动生成且唯一。"),
        "考试安排查询.html": note("汇总本机构送考学员的考试安排，跟踪后续参考信息。", "送考数据来自本机构批次，考试时间、地点和准考信息由评价机构排考后同步。", "仅送考审核通过并完成排考的学员形成考试安排。"),
        "考试安排详情.html": note("集中查看一个送考批次对应的考试时间、地点和学员安排。", "送考名单来源于本机构提交记录，考试批次、考场和准考信息由评价机构产生。", "考试批次编号由系统排考时自动生成，用于唯一标识一次考试安排。"),
        "培训班级.html": note("承接旧版班级入口并统一进入班次管理，不形成独立业务记录。", "页面读取本机构现有课程、班次和学员数据，与班次管理保持一致。", "旧入口不产生新的班级编号或业务状态。"),
        "培训班级详情.html": note("承接旧版班级详情入口并统一查看对应班次记录。", "详情读取班次、学员及培训实施数据，不单独生成班级档案。", "旧入口沿用对应班次编号，不产生新的业务记录。"),
        "送考审核反馈.html": note("承接旧版送考反馈入口，统一查看本机构送考批次的审核结果。", "审核结果由评价机构产生，当前状态与送考批次记录保持一致。", "一期反馈已并入送考批次，不再形成独立反馈业务。"),
        "送考审核反馈详情.html": note("承接旧版反馈详情入口，查看批次退回原因及补充结果。", "批次资料由本机构提交，退回原因由评价机构产生，补充材料由本机构更新。", "一期补充处理统一在送考批次详情完成，不新增独立反馈编号。")
      }
    },
    {
      match: "_评价机构运营端HTML原型_V2",
      publishMatch: "/evaluation/",
      endpoint: "评价机构管理端",
      menus: [
        menu("机构信息", [page("机构信息", [page("编辑机构信息")])]),
        menu("考试项目管理", [page("考试项目列表", [page("考试项目详情"), page("编辑考试项目")])]),
        menu("送考审核", [route("送考批次审核", "送考批次台账.html", [page("送考批次详情")])]),
        menu("考务管理", [
          page("考生名单", [page("考生详情")]),
          page("考试批次", [page("考试批次详情"), page("编辑考试批次")]),
          page("准考证管理", [page("准考证详情")])
        ]),
        menu("考试任务", [page("考试任务", [page("考试任务详情"), page("录入考试结果")])]),
        menu("成绩与证书", [
          page("成绩管理", [page("成绩详情")]),
          page("成绩发布", [page("成绩发布详情")]),
          page("合格考生", [page("合格考生详情")]),
          page("证书模板", [page("编辑证书模板")]),
          page("证书发放", [page("证书发放详情")])
        ])
      ],
      notes: {
        "机构信息.html": note("集中查看本评价机构主体、资质、管理员和服务范围。", "机构基础资料由平台运营端维护，本机构服务信息随机构维护结果更新。", "机构编号由系统生成并唯一标识本评价机构；服务状态影响项目、审核和考务办理。"),
        "编辑机构信息.html": note("维护本评价机构可自行更新的服务、资质和联系信息。", "原数据来自机构主档，本次变更由评价机构管理员录入并保存。", "机构主体编号保持不变，已认证或平台维护字段按现有权限只读。"),
        "考试项目列表.html": note("管理本机构考试项目及其有效状态，为送考审核和排考提供项目依据。", "项目由本机构创建维护，关联送考和考试批次数量由后续业务更新。", "考试项目编号由系统创建项目时自动生成，用于唯一标识一个考试项目。"),
        "考试项目详情.html": note("集中查看一个考试项目的适用工种、考试要求和当前状态。", "项目内容由本机构维护，关联送考批次和考试安排由后续业务产生。", "考试项目编号由系统自动生成并唯一标识该项目；仅有效项目可承接送考。"),
        "编辑考试项目.html": note("新增或维护考试项目，形成送考审核和考试组织使用的项目记录。", "工种、考试要求、成绩规则和服务信息由本机构录入维护。", "新增时由系统生成唯一考试项目编号；已被有效批次使用的关键配置按状态限制修改。"),
        "送考批次台账.html": note("审核培训机构提交的送考批次，跟踪待审核、待补充和审核完成结果。", "批次、考生和附件由培训机构提交，逐人及批次审核结果由本机构产生。", "送考批次编号由系统创建时自动生成并唯一标识一次送考业务。"),
        "送考批次详情.html": note("集中审核一次送考业务的考生及附件，并形成批次审核结论。", "批次资料由培训机构提交，逐人审核意见和批次结果由本机构维护。", "全部考生形成审核结果后方可完成批次审核；批次编号由系统自动生成且唯一。"),
        "考生名单.html": note("汇总进入本机构送考审核和考务范围的考生，跟踪资格审核结果。", "考生来自培训机构提交的送考批次，人员资料和附件沿用送考记录，审核结果由本机构更新。", "每名考生按平台报名编号关联原报名和培训记录。"),
        "考生详情.html": note("集中查看一名考生的身份、培训完成记录和送考附件，形成资格审核依据。", "身份及报名信息来自人员主档，培训结果由培训机构确认，送考附件由培训机构提交。", "审核结论回写考生及所属送考批次；平台报名编号唯一关联本次业务。"),
        "考试批次.html": note("管理本机构考试批次及排考状态，组织审核通过考生参加考试。", "考试项目来自本机构有效项目，考生来自审核完成的送考批次，考场安排由本机构维护。", "考试批次编号由系统创建批次时自动生成，用于唯一标识一次考试组织。"),
        "考试批次详情.html": note("集中查看一次考试批次的项目、考生、时间和考场安排。", "考试项目和考生来自已审核送考记录，考试时间及考场由本机构排考产生。", "考试批次编号由系统自动生成并唯一标识该批次。"),
        "编辑考试批次.html": note("从审核通过的送考记录中组织考生，形成或调整考试安排。", "考试项目来自本机构有效项目，考生来自审核完成的送考批次，时间和考场由本机构录入。", "仅审核通过且未进入其他有效考试批次的考生可纳入；新增时系统生成唯一考试批次编号。"),
        "准考证管理.html": note("管理已排考考生的准考信息，为考生参加考试提供身份和考场凭证。", "准考信息由考试批次、考生身份和考场安排生成，状态随排考调整更新。", "准考证编号由系统生成并唯一对应一名考生的一次考试安排。"),
        "准考证详情.html": note("集中查看一名考生本次考试的准考和考场信息。", "考生身份来自送考记录，考试时间、地点和考场来自考试批次安排。", "准考证编号由系统自动生成并唯一标识本次准考记录。"),
        "考试任务.html": note("汇总已排定、待执行或已完成的考试任务，衔接考试结果录入。", "任务由考试批次及考场安排产生，执行状态由开考、结果录入和完成动作更新。", "考试任务编号由系统生成并唯一标识一次考试执行任务。"),
        "考试任务详情.html": note("集中查看一次考试任务的考生范围和执行结果。", "任务来源于考试批次，考生和考场沿用排考结果，完成进度由考试结果录入更新。", "考试任务编号由系统自动生成并唯一标识该任务。"),
        "录入考试结果.html": note("记录考试任务内考生的理论、实操成绩和合格结论，形成待发布成绩。", "考生来自当前考试任务，成绩和缺考等结果由本机构依据考试执行情况录入。", "准考证编号唯一关联本次参考记录；同一考生同一任务只保留一份有效结果。"),
        "成绩管理.html": note("管理已形成的考试成绩及其校核、发布状态。", "成绩由考试任务结果录入产生，发布状态和复核结果由后续办理更新。", "成绩按考试批次和平台报名编号关联考生，不重复生成同次考试成绩。"),
        "成绩详情.html": note("集中查看一名考生一次考试的成绩、合格结论和复核结果。", "科目成绩来自考试结果录入，发布和复核信息由本机构后续业务更新。", "成绩记录按考试批次和平台报名编号唯一关联本次考试。"),
        "成绩发布.html": note("发布已完成录入和校核的考试成绩，使考生可查询正式结果。", "待发布成绩来自考试任务结果，发布人和发布时间由本机构发布操作产生。", "仅数据完整且尚未发布的成绩可发布；发布后原则上不直接修改原成绩。"),
        "成绩发布详情.html": note("集中查看一次成绩发布的考生范围、结果和发布时间。", "发布范围来自对应考试批次的已校核成绩，发布记录由本机构产生。", "考试批次编号唯一关联本次发布范围；同一成绩不重复发布。"),
        "合格考生.html": note("汇总成绩已发布且满足发证条件的考生，为证书生成提供人员范围。", "合格结论来自已发布成绩，人员身份来自报名主档，证书状态由发放业务更新。", "平台报名编号关联原报名与考试；仅成绩合格且身份完整的考生进入。"),
        "合格考生详情.html": note("集中查看一名合格考生的成绩依据、身份信息和证书状态。", "人员信息来自报名主档，合格结论来自已发布成绩，证书信息由发放环节产生。", "平台报名编号关联原报名和考试记录，已发证人员不得重复生成同类证书。"),
        "证书模板.html": note("管理本机构证书模板及启用状态，为合格考生生成证书提供版式依据。", "模板内容和适用项目由本机构维护，使用状态随启用或停用操作更新。", "生成证书时仅使用当前有效且启用的对应模板。"),
        "编辑证书模板.html": note("新增或维护证书模板及其适用项目和证书字段。", "模板版式、字段和适用范围由本机构录入维护。", "模板编号由系统创建时自动生成；模板变更不改变已发放证书内容。"),
        "证书发放.html": note("为符合条件的合格考生生成并发放证书，完成一期取证闭环。", "考生和合格结论来自已发布成绩，模板来自启用中的证书模板，发放记录由本机构产生。", "证书编号由系统生成并唯一标识一份证书；已发放考生不得重复发放同一证书。"),
        "证书发放详情.html": note("集中查看一次证书生成与发放结果及关联考生。", "考生和成绩来自合格考生记录，模板和证书信息来自本次发放业务。", "证书编号由系统自动生成并唯一标识该证书；发放后同步个人及平台证书档案。")
      }
    }
  ];

  function moduleNote(name, display, data, interaction, state, open) {
    return {
      name: name,
      display: display,
      data: data,
      interaction: interaction,
      state: state,
      open: Boolean(open)
    };
  }

  function actionNote(name, trigger, validation, success, failure) {
    return { name: name, trigger: trigger, validation: validation, success: success, failure: failure };
  }

  function sourceFor(endpoint, title) {
    if (endpoint === "PC端") {
      if (/课程|培训机构/.test(title)) return "培训机构端已发布课程、班次及机构资料，并结合平台审核发布状态";
      if (/报名|订单|支付/.test(title)) return "用户提交的报名主档、培训机构受理结果及订单支付或核款记录";
      if (/在线课程|线下培训|我的培训|培训过程/.test(title)) return "培训机构端班次、课程学习、签到、实训及完成结果";
      if (/考试|评价|成绩|证书/.test(title)) return "评价机构端考试项目、排考、成绩发布、复核及证书发放记录";
      if (/个人/.test(title)) return "登录用户实名认证资料和平台报名主档";
      return "培训考试专区当前有效的业务数据";
    }
    if (endpoint === "平台运营管理端") {
      if (/Banner|通知|政策/.test(title)) return "平台运营端专区内容配置及发布记录";
      if (/课程|班次/.test(title)) return "培训机构提交的课程和班次数据及平台审核结果";
      if (/考试项目|考试管理|考试详情/.test(title)) return "评价机构提交的考试项目及平台审核结果";
      if (/培训机构|评价机构/.test(title)) return "机构入驻资料、资质材料和平台维护状态";
      if (/报名/.test(title)) return "PC端形成的个人及团队报名主档";
      if (/订单/.test(title)) return "报名订单、支付结果和培训机构核款记录";
      if (/送考/.test(title)) return "培训机构送考批次及评价机构审核、排考反馈";
      if (/档案|一人一档/.test(title)) return "报名、培训、考试和证书业务记录的汇总档案";
      return "平台运营端当前业务记录";
    }
    if (endpoint === "培训机构管理端") {
      if (/机构信息/.test(title)) return "本机构入驻资料、资质及收款主体信息";
      if (/课程/.test(title)) return "本机构维护的课程及平台审核发布结果";
      if (/报名|成员/.test(title)) return "PC端提交至本机构的个人和团队报名资料";
      if (/订单/.test(title)) return "本机构关联报名订单、在线支付及对公核款记录";
      if (/班次|培训过程|学员档案/.test(title)) return "本机构班次、学员、线上学时、线下出勤和实训记录";
      if (/培训完成/.test(title)) return "达到课程学时、出勤和实训要求的培训完成记录";
      if (/送考/.test(title)) return "本机构创建的送考批次、学员附件及评价机构反馈";
      if (/考试安排/.test(title)) return "评价机构同步的考试批次、考场和准考信息";
      return "培训机构端当前业务记录";
    }
    if (endpoint === "评价机构管理端") {
      if (/机构信息/.test(title)) return "本评价机构入驻资料、资质及服务信息";
      if (/考试项目/.test(title)) return "本机构维护的考试项目及平台审核发布结果";
      if (/送考/.test(title)) return "培训机构提交的送考批次、考生资料和附件";
      if (/考生/.test(title)) return "已进入本机构送考审核或排考范围的考生记录";
      if (/考试批次|准考证/.test(title)) return "审核通过的送考批次及本机构排考结果";
      if (/考试任务|录入考试结果/.test(title)) return "已排考批次生成的考试执行任务及结果记录";
      if (/成绩/.test(title)) return "考试结果、成绩校核、发布和复核记录";
      if (/合格考生|证书/.test(title)) return "已发布成绩、合格结论、证书模板及发放记录";
      return "评价机构端当前业务记录";
    }
    return "当前页面关联业务数据";
  }

  function identityRule(title) {
    if (/Banner|通知|政策/.test(title)) return "以内容编号、发布状态和有效期识别展示版本";
    if (/课程/.test(title)) return "以课程编号关联机构、班次和审核发布状态";
    if (/班次/.test(title)) return "以班次编号关联课程、学员和培训过程";
    if (/报名/.test(title)) return "以平台报名编号关联人员、课程、机构和办理状态";
    if (/订单|支付/.test(title)) return "以订单编号和平台报名编号关联费用与支付结果";
    if (/送考/.test(title)) return "以送考批次号关联工种、评价机构、学员和审核状态";
    if (/考试项目/.test(title)) return "以考试项目编号关联工种、评价机构和发布状态";
    if (/考试批次|考试任务|准考证|考试安排/.test(title)) return "以考试批次号和平台报名编号关联考生与考场安排";
    if (/成绩/.test(title)) return "以考试批次号和平台报名编号关联成绩及发布状态";
    if (/证书/.test(title)) return "以证书编号和平台报名编号关联人员、成绩与发放记录";
    if (/机构/.test(title)) return "以机构编号关联资质、服务状态和业务数据";
    if (/档案|学员/.test(title)) return "以人员身份标识和平台报名编号汇总过程记录";
    return "以当前业务编号读取唯一记录并保持上下游状态一致";
  }

  function entityName(title) {
    var entity = title
      .replace(/(管理|列表|台账|查询|审核|详情|编辑|创建|录入|发布|发放)$/g, "")
      .replace(/^(个人|团队)/, "");
    return entity || "业务";
  }

  function roleFor(endpoint, title) {
    if (endpoint === "PC端") {
      if (/首页|培训课程$|课程详情|培训机构|考试服务$|评价项目详情|证书真伪查询/.test(title)) return "个人用户、企业经办人及访客；个人业务数据仅本人登录后可见。";
      return "已登录个人用户；仅查看和办理本人身份及平台报名编号关联的数据。";
    }
    if (endpoint === "平台运营管理端") return "平台运营管理员；查看全平台授权范围内的机构、报名及业务档案。";
    if (endpoint === "培训机构管理端") return "培训机构管理员；仅处理本机构课程、报名、订单、培训和送考数据。";
    return "评价机构管理员；仅处理本机构考试项目、送考审核、考务、成绩和证书数据。";
  }

  function prerequisiteFor(endpoint, title) {
    if (endpoint === "PC端" && /首页|培训课程$|课程详情|培训机构|考试服务$|评价项目详情|证书真伪查询/.test(title)) return "公开页面可直接访问；进入报名、订单、学习、成绩或证书个人记录前需完成登录。";
    if (/报名订单支付/.test(title)) return "报名已受理并生成待支付订单，且订单仍在有效期内。";
    if (/在线课程学习|线下培训日程签到/.test(title)) return "报名已受理且完成缴费，培训机构已创建并开放对应班次任务。";
    if (/成绩查询复核|证书与档案/.test(title)) return "评价机构已发布对应考试成绩；证书内容还需满足合格及发放条件。";
    if (/编辑|创建|录入/.test(title)) return "当前用户具有维护权限，且业务记录处于允许新增、编辑或补充的状态。";
    if (/审核/.test(title)) return "上游机构已提交待审核记录及一期要求的必要材料。";
    if (/详情|信息$|考试安排$/.test(title)) return "从对应列表、业务入口或关联记录选择一条有效数据进入。";
    return endpoint === "PC端" ? "用户按页面要求完成登录或查询条件后进入。" : "管理员已登录并具有当前菜单的数据权限。";
  }

  function resultFor(endpoint, title, baseNote) {
    if (/查询|列表|管理$|台账|档案$|我的培训考试|我的培训$|考试服务$/.test(title)) return "形成可继续查看或办理的结果集，不直接改变原业务状态。";
    if (/编辑|创建|录入|报名|支付|审核|发布|发放/.test(title)) return baseNote.data + "；成功后同步刷新关联列表、详情和上下游状态。";
    if (/详情|信息$|考试安排$/.test(title)) return "以只读方式还原当前业务记录；发生有效操作后回写当前记录并同步关联页面。";
    return baseNote.data;
  }

  function interactionForModule(name) {
    if (/导航|入口|下一步/.test(name)) return "点击有效入口进入对应页面；涉及个人业务时先校验登录态和数据权限。";
    if (/Banner/.test(name)) return "支持轮播切换；点击后按后台配置跳转站内目标页面。";
    if (/筛选|查询条件/.test(name)) return "提交条件刷新当前结果；重置清空本次筛选并恢复默认数据范围。";
    if (/列表|明细|记录|资料/.test(name)) return "点击查看进入对应详情；可执行项根据当前记录状态动态显示。";
    if (/配置|基础信息|字段|材料|付款/.test(name)) return "按页面字段录入、选择或上传；提交前执行必填和业务条件校验。";
    if (/状态|进度|统计|概览/.test(name)) return "切换状态仅改变当前展示口径，不直接修改业务记录。";
    return "按模块内按钮进入对应一期主流程；操作结果返回当前页或关联详情。";
  }

  function stateForModule(name) {
    if (/Banner|通知|政策|课程|计划/.test(name)) return "仅展示已发布且处于有效期内的数据；无有效数据时隐藏模块或展示空状态。";
    if (/筛选|列表|查询|明细|记录/.test(name)) return "覆盖正常结果和无数据状态；加载、接口异常等技术状态不在静态原型中展开。";
    if (/配置|字段|材料|付款/.test(name)) return "必填项未完成或校验失败时阻止提交并保留已填写内容；只读状态不可修改。";
    if (/状态|进度|统计/.test(name)) return "数量与明细使用同一口径；业务状态变化后重新汇总展示。";
    return "按权限和业务状态展示可用、只读、禁用或空状态。";
  }

  function normalizeModules(modules) {
    return modules.map(function (item) {
      return moduleNote(
        item.name,
        item.display || item.core,
        item.data,
        item.interaction || interactionForModule(item.name),
        item.state || stateForModule(item.name),
        item.open
      );
    });
  }

  function actionsFor(endpoint, title, entity, baseNote) {
    if (/送考批次详情/.test(title) && endpoint === "培训机构管理端") return [
      actionNote("查看学员送考资料", "管理员在学员明细中选择一名送考学员。", "校验学员属于当前批次且附件记录存在。", "在弹窗中展示该学员送考附件及当前审核结果。", "资料不存在时展示未提交状态，不生成模拟附件。"),
      actionNote("补充送考资料", "当前批次或学员状态为待补充。", "校验退回原因、需补充项及新附件完整性。", "更新附件版本并重新提交评价机构审核。", "非待补充状态只允许查看，不允许覆盖已审核资料。")
    ];
    if (/送考批次详情/.test(title) && endpoint === "评价机构管理端") return [
      actionNote("查看并审核学员", "审核人选择当前批次中的送考学员。", "校验学员身份、送考附件和当前待审核状态。", "记录逐人审核结论，并实时汇总批次通过、待审和待补充人数。", "资料不足时退回补充且必须填写原因。"),
      actionNote("完成批次审核", "当前批次全部学员均已有审核结论。", "校验不存在待审核学员并确认批次统计。", "批次进入审核完成并可被纳入后续排考。", "仍有待审核记录时阻止完成批次审核。")
    ];
    if (/送考批次详情/.test(title) && endpoint === "平台运营管理端") return [
      actionNote("查看流转记录", "管理员进入送考批次详情。", "校验批次存在且属于平台监管范围。", "只读展示培训机构提交、评价机构审核及排考反馈。", "平台端不代替机构修改送考资料或审核结论。")
    ];
    if (/报名订单支付/.test(title)) return [
      actionNote("提交付款", "用户选择支付方式并填写对应付款信息。", "校验订单有效期、应付金额及必填凭证字段。", "生成支付或待核款记录，并同步报名缴费状态。", "校验失败时不提交，提示需补充或修正的字段。")
    ];
    if (/审核/.test(title)) return [
      actionNote("审核通过", "审核人确认主体资料及附件符合要求。", "校验审核意见、必要材料和当前待审核状态。", "记录审核人及时间，业务进入下一办理状态。", "条件不足时不得通过，可选择退回补充并填写原因。"),
      actionNote("退回补充", "发现资料缺失或内容不符合要求。", "必须填写明确退回原因和需补充项。", "状态更新为待补充并同步提交方。", "未填写退回原因时阻止提交。")
    ];
    if (/成绩发布/.test(title)) return [
      actionNote("发布成绩", "选择已完成录入和校核的考试批次。", "校验批次成绩完整、合格结论已生成且尚未发布。", "成绩对考生开放查询并进入合格考生统计。", "存在缺失或异常成绩时阻止整批发布。")
    ];
    if (/证书发放/.test(title)) return [
      actionNote("生成并发放证书", "选择成绩已发布且符合发证条件的考生。", "校验证书模板启用、人员身份完整且证书编号未重复。", "生成证书记录并同步PC端及平台证书档案。", "任一条件不满足时保留待处理状态并提示原因。")
    ];
    if (/录入考试结果/.test(title)) return [
      actionNote("保存考试结果", "评价机构录入考生成绩与合格结论。", "校验考生属于当前考试任务、成绩格式有效且无重复记录。", "形成待发布成绩记录并更新考试任务进度。", "校验失败时保留录入内容并标记错误项。")
    ];
    if (/编辑|创建|个人培训报名|个人报名资料/.test(title)) return [
      actionNote("保存/提交", "用户完成当前页面一期核心字段。", "校验必填、格式、关联数据有效性及当前可编辑状态。", baseNote.data + "；成功后返回详情或进入下一流程。", "校验失败时停留当前页并提示具体字段，不改变原业务状态。")
    ];
    if (/查询|列表|管理$|台账|档案$/.test(title)) return [
      actionNote("查询", "用户输入关键词或选择筛选条件。", "按当前角色数据范围组合查询条件。", "刷新统计与列表，保留本次筛选条件。", "无匹配数据时展示空结果，不改变原始数据。"),
      actionNote("查看", "用户选择一条可访问记录。", "校验记录存在且在当前角色权限范围内。", "进入对应详情并带入唯一业务编号。", "记录不可用时停留列表并提示。")
    ];
    if (/详情|信息$|考试安排$|我的培训考试|我的培训$|考试服务$/.test(title)) return [
      actionNote("进入关联业务", "用户点击页面内当前状态允许的入口。", "校验登录角色、业务状态和关联记录是否有效。", "进入对应列表、详情或下一办理页面。", "无权限或无关联数据时不跳转并给出提示。")
    ];
    return [actionNote("页面操作", "用户触发页面一期已展示的有效操作。", "校验当前角色、记录状态和必要字段。", "完成当前主流程并同步关联数据。", "条件不足时不改变原记录并提示原因。")];
  }

  function enrichNote(endpoint, title, baseNote) {
    var source = sourceFor(endpoint, title);
    var identity = identityRule(title);
    var entity = entityName(title);
    var overview = {
      target: baseNote.core,
      role: roleFor(endpoint, title),
      prerequisite: prerequisiteFor(endpoint, title),
      result: resultFor(endpoint, title, baseNote)
    };
    var modules;

    if (baseNote.modules && baseNote.modules.length) {
      modules = normalizeModules(baseNote.modules);
    } else if (/报名订单支付|团队报名订单支付/.test(title)) {
      modules = [
        moduleNote("订单信息", "展示报名对象、课程班次、收款主体和订单有效期。", source + "；" + identity + "。", "可从订单信息返回报名详情，不允许在支付页修改订单主体。", "仅待支付且未过期订单可继续办理；过期或已支付时入口禁用。", true),
        moduleNote("费用明细", "展示培训费、考试费及本次应付合计。", "费用由受理通过后的报名数据生成，订单金额作为支付与核款基准。", "费用只读；如有异议返回报名订单详情核对。", "费用未生成或订单金额异常时不得提交付款。"),
        moduleNote("支付方式", "选择在线支付或线下对公汇款。", "支付方式决定后续必填字段和订单状态。", "切换方式同步切换付款字段；一期演示正常支付及凭证提交。", "未选择方式时提交按钮不可用。"),
        moduleNote("付款材料", "填写付款主体、时间、汇款金额、流水号并上传凭证。", "线下汇款材料随订单编号保存并进入培训机构核款。", "上传后可替换凭证；提交时校验金额、流水号和文件。", "字段缺失或格式不符时阻止提交并定位错误项。"),
        moduleNote("支付结果", "反馈在线支付成功或线下凭证提交结果。", baseNote.data + "；成功结果同步报名缴费状态。", "成功后进入支付结果或订单详情；重复提交不生成新订单。", "支付失败保持待支付；线下提交后显示核款中。")
      ];
    } else if (/在线课程学习|线下培训日程签到/.test(title)) {
      modules = [
        moduleNote("培训任务", "展示当前课程、班次、培训机构和培训时间。", source + "；" + identity + "。", "从任务切换到对应章节或课次，任务主体不可在本页修改。", "仅已开放且本人已入班的任务可访问。", true),
        moduleNote("学习安排", "展示在线章节或线下培训课次、时间和完成要求。", "仅读取当前班次已发布且处于有效期内的教学安排。", "点击章节开始学习；线下课次在签到时段内开放签到。", "未开放、已结束或已完成内容显示相应只读状态。"),
        moduleNote("过程记录", "记录章节完成、学习时长或线下签到结果。", "每次学习和签到写入当前学员培训过程，不修改课程基础信息。", "完成动作后即时刷新当前课次及累计进度。", "重复签到不重复计数；不在有效时段内阻止操作。"),
        moduleNote("完成进度", "汇总已完成内容、累计学时和未完成要求。", baseNote.data + "；统计口径与培训完成判定一致。", "查看各项明细，不允许手工修改累计结果。", "任一必修项未达标时保持培训中。"),
        moduleNote("后续办理", "达到培训要求后显示培训完成及后续送考提示。", "培训状态由线上、线下和实训过程数据汇总更新。", "达标后进入培训完成结果；送考由培训机构后续发起。", "未达标时不进入培训完成名单。")
      ];
    } else if (/报名支付成功|培训过程完成结果/.test(title)) {
      modules = [
        moduleNote("结果反馈", "展示本次办理结果、关键编号和完成时间。", source + "；读取刚完成业务的最终状态。", "结果页只提供查看和后续入口，不重复执行原提交动作。", "仅展示成功或已完成结果；失败仍停留原办理页。", true),
        moduleNote("业务更新", "说明结果对订单、报名或培训状态的影响。", baseNote.data + "；结果页不重复生成业务记录。", "返回详情可查看更新后的完整过程。", "上下游状态以同一业务编号保持一致。"),
        moduleNote("关联记录", "展示订单、报名或培训记录的查看入口。", identity + "，保证结果页与详情页读取同一记录。", "点击进入对应详情。", "关联记录不存在时隐藏入口并提示。"),
        moduleNote("下一步入口", "引导进入培训、考试或个人中心。", "仅展示当前状态允许的一期下一步操作。", "点击后沿核心闭环继续办理。", "不满足下一步条件时仅显示结果说明。")
      ];
    } else if (/证书真伪查询|成绩查询复核/.test(title)) {
      modules = [
        moduleNote("查询条件", "填写证书编号、人员身份或考试相关条件。", "输入项用于匹配" + source + "。", "提交条件执行查询，重置后清空当前结果。", "必填条件不完整时不发起查询。", true),
        moduleNote("查询处理", "校验是否存在唯一对应业务记录。", identity + "；查询不改变原业务状态。", "匹配成功展示结果，未匹配返回空状态。", "条件冲突或记录无权限时不返回业务明细。"),
        moduleNote("结果展示", "展示成绩或证书当前状态和关键业务信息。", baseNote.data + "。", "可进入复核、证书档案或公开查验详情。", "未发布、已撤销或不存在时显示明确状态说明。"),
        moduleNote("关联办理", "按查询结果提供复核或档案入口。", "入口由成绩发布时间、复核期限和证书状态控制。", "提交复核后生成复核记录并可查看处理状态。", "超出复核期或不符合条件时入口禁用。")
      ];
    } else if (/编辑|创建|录入|个人培训报名|个人报名资料/.test(title)) {
      modules = [
        moduleNote(entity + "基础信息", "填写或核对主体、编号和所属业务。", source + "；编辑时读取原记录，新增时按当前上下文预填。", "可编辑字段允许录入；系统生成编号和已认证字段只读。", "新增显示空值或默认值，编辑显示最新有效版本。", true),
        moduleNote(entity + "业务配置", "维护当前页面一期办理所需的核心字段。", identity + "；关联字段仅从上游有效数据中选择。", "选择关联项后联动带出主体、工种、费用或状态信息。", "关联数据失效时阻止提交并要求重新选择。"),
        moduleNote("材料附件", "上传或替换当前业务要求的证明材料。", "附件随当前业务编号保存；无材料要求的页面不生成额外附件。", "支持选择、预览、替换和删除未提交附件。", "已提交或只读状态仅允许预览；缺少必传材料时阻止提交。"),
        moduleNote("字段校验", "校验必填项、格式、唯一性和关键业务条件。", "校验覆盖原型已展示字段及一期核心业务规则。", "失焦或提交时提示具体错误位置。", "未通过时保留已填内容且不改变原记录。"),
        moduleNote("保存提交", "保存草稿或提交进入下一办理环节。", baseNote.data + "；提交后按状态限制关键字段再次编辑。", "保存草稿不推进状态；提交成功返回详情或列表。", "重复提交不生成重复业务记录。")
      ];
    } else if (/详情|机构信息$|考试安排$/.test(title)) {
      modules = [
        moduleNote(entity + "基础信息", "展示编号、主体、所属业务和关键属性。", source + "；" + identity + "。", "从关联编号可进入上游或下游详情。", "记录失效或超出权限时不展示业务内容。", true),
        moduleNote("办理状态", "展示当前状态、办理进度和关键时间节点。", baseNote.data + "；状态由上游提交和当前端办理结果共同更新。", "状态节点用于查看，不在普通详情区直接修改。", "当前节点高亮，未发生节点显示待办理。"),
        moduleNote(entity + "业务明细", "展示关联人员、费用、课程、班次或考试明细。", "明细与主记录使用同一业务编号，不重复创建。", "列表型明细支持查看单条详情或附件。", "无关联明细时展示空状态，不补造演示数据。"),
        moduleNote("关联资料", "展示已提交附件、凭证和过程记录。", "读取当前业务最新有效版本并保留历史办理结果。", "支持预览或下载；有权限且状态允许时可补充。", "无材料时展示未提交；只读状态禁止替换。"),
        moduleNote("可执行操作", "按当前状态提供编辑、审核、下载或下一步入口。", "权限由登录角色和业务状态共同控制。", "操作完成后回写当前记录并刷新详情。", "条件不满足时隐藏或禁用操作并说明原因。")
      ];
    } else if (/首页|我的培训考试|考试服务$|我的培训$|证书与档案/.test(title)) {
      modules = [
        moduleNote("业务概览", "汇总当前用户可办理或查看的一期核心业务。", source + "；按登录角色和数据权限展示。", "点击业务卡片进入对应列表或详情。", "未登录或无业务数据时展示引导或空状态。", true),
        moduleNote("状态摘要", "展示待办理、进行中和已完成业务数量。", "统计与下方业务入口使用同一数据口径。", "切换状态筛选当前内容，不修改业务状态。", "业务状态变化后重新汇总数量。"),
        moduleNote("功能入口", "按培训、考试、订单或证书分类提供入口。", "入口根据权限和当前业务条件生成。", "点击后带入登录人和关联业务编号。", "无权限或无关联记录时隐藏入口或提示。"),
        moduleNote("数据范围", "说明当前页面可查看的人员和业务范围。", baseNote.data + "；个人端仅展示登录用户关联记录。", "页面不提供跨用户切换。", "退出登录后清除个人业务内容。")
      ];
    } else {
      modules = [
        moduleNote(entity + "状态统计", "汇总不同办理状态及数量。", source + "；统计与当前列表使用同一查询口径。", "点击状态切换当前列表范围。", "数量随筛选和业务状态同步变化。", true),
        moduleNote(entity + "查询筛选", "按关键词、主体、时间或状态筛选。", "筛选条件只作用于当前角色可见数据。", "搜索刷新列表；重置恢复默认数据范围。", "无匹配数据时展示空结果。"),
        moduleNote(entity + "列表", "展示关键字段、状态、更新时间和责任主体。", source + "；" + identity + "；" + baseNote.data, "点击记录进入详情；操作列随状态展示。", "正常、空数据和只读状态均有明确反馈。"),
        moduleNote(entity + "业务操作", "提供查看、编辑、审核或办理入口。", "操作权限由登录角色和记录状态决定。", "进入详情继续完成一期主流程。", "不可操作记录仅保留查看或不展示入口。"),
        moduleNote("分页统计", "展示符合条件的数据总数和分页结果。", "分页沿用当前筛选条件，一期使用静态演示数据。", "切换页码不改变业务状态。", "无数据时总数为0并禁用分页。")
      ];
    }

    return {
      core: baseNote.core,
      data: baseNote.data,
      overview: overview,
      modules: modules,
      actions: actionsFor(endpoint, title, entity, baseNote)
    };
  }

  var decodedPath;
  try { decodedPath = decodeURIComponent(location.pathname); } catch (error) { decodedPath = location.pathname; }
  var config = CONFIGS.find(function (item) {
    return decodedPath.indexOf(item.match) !== -1 || (item.publishMatch && decodedPath.indexOf(item.publishMatch) !== -1);
  });
  if (!config) return;

  var currentFile = decodedPath.split(/[\\/]/).pop() || "";
  var currentNote = config.notes[currentFile] || note(
    "承载当前页面对应的一期核心业务。",
    "数据来源于当前角色可办理的业务记录，并由对应业务环节更新。"
  );
  var currentTitle = currentFile.replace(/\.html$/i, "") || document.title || "当前页面";
  var iconMenu = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M4 6h16M4 12h16M4 18h16"/></svg>';
  var iconInfo = '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="9"/><path d="M12 11v6M12 7h.01"/></svg>';

  var dock = document.createElement("div");
  dock.className = "prototype-tools-dock";
  dock.setAttribute("aria-label", "原型辅助工具");
  dock.innerHTML =
    '<button class="prototype-tools-button" type="button" data-tool="menu" aria-expanded="false">' + iconMenu + '<span>页面导航</span></button>' +
    '<button class="prototype-tools-button" type="button" data-tool="info" aria-expanded="false">' + iconInfo + '<span>功能说明</span></button>';

  var panel = document.createElement("aside");
  panel.className = "prototype-tools-panel";
  panel.setAttribute("aria-live", "polite");
  panel.innerHTML =
    '<div class="prototype-tools-panel-header">' +
      '<div><h2 class="prototype-tools-panel-title"></h2><p class="prototype-tools-panel-subtitle"></p></div>' +
      '<button class="prototype-tools-close" type="button" aria-label="关闭">&times;</button>' +
    '</div>' +
    '<div class="prototype-tools-panel-body"></div>';

  document.body.appendChild(dock);
  document.body.appendChild(panel);

  var titleNode = panel.querySelector(".prototype-tools-panel-title");
  var subtitleNode = panel.querySelector(".prototype-tools-panel-subtitle");
  var bodyNode = panel.querySelector(".prototype-tools-panel-body");
  var closeButton = panel.querySelector(".prototype-tools-close");
  var buttons = Array.prototype.slice.call(dock.querySelectorAll(".prototype-tools-button"));
  var activeTool = null;
  var suppressClickUntil = 0;

  function renderMenu() {
    panel.classList.remove("is-detailed");
    titleNode.textContent = "页面导航";
    subtitleNode.textContent = config.endpoint + " · 当前：" + currentTitle;
    bodyNode.innerHTML = "";
    var search = document.createElement("input");
    search.className = "prototype-tools-search";
    search.type = "search";
    search.placeholder = "搜索页面名称";
    search.setAttribute("aria-label", "搜索页面名称");
    bodyNode.appendChild(search);

    var groupsWrap = document.createElement("div");
    var hasCurrentGroup = false;

    function isCurrentPage(item) {
      if (item.matchCurrent === false || !item.file) return false;
      var target;
      try { target = new URL(item.file, location.href); } catch (error) { return false; }
      var targetFile;
      try { targetFile = decodeURIComponent(target.pathname).split(/[\\/]/).pop() || ""; }
      catch (error) { targetFile = target.pathname.split(/[\\/]/).pop() || ""; }
      if (targetFile !== currentFile) return false;
      if (item.defaultCurrent && !location.search && !location.hash) return true;
      if (item.file.indexOf("?") !== -1 && target.search !== location.search) return false;
      if (item.file.indexOf("#") !== -1 && target.hash !== location.hash) return false;
      return true;
    }

    config.menus.forEach(function (group) {
      var details = document.createElement("details");
      details.className = "prototype-tools-nav-group";
      var summary = document.createElement("summary");
      var flatPages = [];
      (function collect(items) {
        items.forEach(function (item) {
          flatPages.push(item);
          if (item.children && item.children.length) collect(item.children);
        });
      })(group.pages);
      summary.textContent = group.group + "（" + flatPages.length + "）";
      var list = document.createElement("div");
      list.className = "prototype-tools-nav-list";
      var containsCurrent = false;

      function appendPage(page, container, depth) {
        var node = document.createElement("div");
        node.className = "prototype-tools-nav-node";
        node.style.setProperty("--pt-nav-depth", depth);
        var link = document.createElement("a");
        link.className = "prototype-tools-nav-link";
        link.href = page.file;
        link.dataset.pageName = page.name.toLowerCase();
        var label = document.createElement("span");
        label.textContent = page.name;
        link.appendChild(label);
        var current = isCurrentPage(page);
        if (current) {
          containsCurrent = true;
          link.classList.add("is-current");
          link.setAttribute("aria-current", "page");
          var dot = document.createElement("span");
          dot.className = "prototype-tools-current-dot";
          link.appendChild(dot);
        }
        node.appendChild(link);

        var childContainsCurrent = false;
        if (page.children && page.children.length) {
          var children = document.createElement("div");
          children.className = "prototype-tools-nav-children";
          page.children.forEach(function (child) {
            if (appendPage(child, children, depth + 1)) childContainsCurrent = true;
          });
          node.appendChild(children);
        }
        if (childContainsCurrent) {
          node.classList.add("has-current-path");
          link.classList.add("is-current-parent");
        }
        container.appendChild(node);
        return current || childContainsCurrent;
      }

      group.pages.forEach(function (page) { appendPage(page, list, 0); });
      details.appendChild(summary);
      details.appendChild(list);
      if (containsCurrent) {
        details.open = true;
        hasCurrentGroup = true;
      }
      groupsWrap.appendChild(details);
    });
    if (!hasCurrentGroup && groupsWrap.firstElementChild) groupsWrap.firstElementChild.open = true;
    bodyNode.appendChild(groupsWrap);

    var empty = document.createElement("div");
    empty.className = "prototype-tools-empty";
    empty.textContent = "未找到匹配页面";
    empty.hidden = true;
    bodyNode.appendChild(empty);

    search.addEventListener("input", function () {
      var keyword = search.value.trim().toLowerCase();
      var visibleTotal = 0;
      Array.prototype.forEach.call(groupsWrap.querySelectorAll(".prototype-tools-nav-group"), function (details) {
        var nodes = Array.prototype.slice.call(details.querySelectorAll(".prototype-tools-nav-node"));
        if (!keyword) {
          nodes.forEach(function (node) { node.hidden = false; });
        } else {
          nodes.reverse().forEach(function (node) {
            var ownLink = node.querySelector(":scope > .prototype-tools-nav-link");
            var ownMatch = ownLink && ownLink.dataset.pageName.indexOf(keyword) !== -1;
            var childMatch = Boolean(node.querySelector(":scope > .prototype-tools-nav-children > .prototype-tools-nav-node:not([hidden])"));
            node.hidden = !ownMatch && !childMatch;
          });
        }
        var visibleInGroup = 0;
        Array.prototype.forEach.call(details.querySelectorAll(".prototype-tools-nav-node:not([hidden]) > .prototype-tools-nav-link"), function () {
          visibleInGroup += 1;
        });
        details.hidden = visibleInGroup === 0;
        if (keyword && visibleInGroup) details.open = true;
        visibleTotal += visibleInGroup;
      });
      empty.hidden = visibleTotal !== 0;
    });
  }

  function renderInfo() {
    titleNode.textContent = "功能说明";
    subtitleNode.textContent = config.endpoint + " · " + currentTitle;
    panel.classList.remove("is-detailed");
    bodyNode.innerHTML = "";

    var card = document.createElement("section");
    card.className = "prototype-tools-note-card prototype-tools-overview-card";
    var heading = document.createElement("h3");
    heading.className = "prototype-tools-note-label";
    heading.textContent = "页面说明";
    card.appendChild(heading);

    var grid = document.createElement("div");
    grid.className = "prototype-tools-overview-grid";
    var rows = [
      ["核心功能", currentNote.core],
      ["数据来源", currentNote.data]
    ];
    if (currentNote.rules) rows.push(["业务规则", currentNote.rules]);
    rows.forEach(function (row) {
      var item = document.createElement("div");
      item.className = "prototype-tools-overview-row";
      var label = document.createElement("span");
      label.textContent = row[0];
      var value = document.createElement("p");
      value.textContent = row[1];
      item.appendChild(label);
      item.appendChild(value);
      grid.appendChild(item);
    });
    card.appendChild(grid);
    bodyNode.appendChild(card);
  }

  function clamp(value, min, max) {
    return Math.min(Math.max(value, min), Math.max(min, max));
  }

  function positionPanel() {
    if (!activeTool) return;
    var dockRect = dock.getBoundingClientRect();
    var panelRect = panel.getBoundingClientRect();
    var gap = 12;
    var left = dockRect.left > window.innerWidth / 2
      ? dockRect.left - panelRect.width - gap
      : dockRect.right + gap;
    if (left < 16 || left + panelRect.width > window.innerWidth - 16) {
      left = window.innerWidth - panelRect.width - 16;
    }
    var top = clamp(dockRect.top, 16, window.innerHeight - panelRect.height - 16);
    panel.style.left = Math.round(clamp(left, 16, window.innerWidth - panelRect.width - 16)) + "px";
    panel.style.top = Math.round(top) + "px";
  }

  function setActiveTool(tool) {
    activeTool = tool;
    if (!tool) {
      panel.classList.remove("is-visible");
      buttons.forEach(function (button) {
        button.classList.remove("is-active");
        button.setAttribute("aria-expanded", "false");
      });
      return;
    }
    if (tool === "menu") renderMenu();
    if (tool === "info") renderInfo();
    panel.classList.add("is-visible");
    buttons.forEach(function (button) {
      var isActive = button.dataset.tool === tool;
      button.classList.toggle("is-active", isActive);
      button.setAttribute("aria-expanded", isActive ? "true" : "false");
    });
    requestAnimationFrame(positionPanel);
  }

  buttons.forEach(function (button) {
    button.addEventListener("click", function () {
      if (Date.now() < suppressClickUntil) return;
      setActiveTool(activeTool === button.dataset.tool ? null : button.dataset.tool);
    });
  });
  closeButton.addEventListener("click", function () { setActiveTool(null); });
  document.addEventListener("keydown", function (event) {
    if (event.key === "Escape" && activeTool) setActiveTool(null);
  });

  var drag = null;
  dock.addEventListener("pointerdown", function (event) {
    if (event.button !== 0) return;
    var rect = dock.getBoundingClientRect();
    drag = {
      pointerId: event.pointerId,
      startX: event.clientX,
      startY: event.clientY,
      left: rect.left,
      top: rect.top,
      moved: false
    };
  });

  window.addEventListener("pointermove", function (event) {
    if (!drag || drag.pointerId !== event.pointerId) return;
    var dx = event.clientX - drag.startX;
    var dy = event.clientY - drag.startY;
    if (!drag.moved && Math.hypot(dx, dy) < 6) return;
    drag.moved = true;
    dock.classList.add("is-dragging");
    var rect = dock.getBoundingClientRect();
    dock.style.left = clamp(drag.left + dx, 8, window.innerWidth - rect.width - 8) + "px";
    dock.style.top = clamp(drag.top + dy, 8, window.innerHeight - rect.height - 8) + "px";
    dock.style.right = "auto";
    dock.style.bottom = "auto";
    event.preventDefault();
    positionPanel();
  });

  function finishDrag(event) {
    if (!drag || drag.pointerId !== event.pointerId) return;
    if (drag.moved) {
      suppressClickUntil = Date.now() + 260;
    }
    dock.classList.remove("is-dragging");
    drag = null;
  }
  window.addEventListener("pointerup", finishDrag);
  window.addEventListener("pointercancel", finishDrag);

  window.addEventListener("resize", function () {
    var rect = dock.getBoundingClientRect();
    if (dock.style.left) {
      dock.style.left = clamp(rect.left, 8, window.innerWidth - rect.width - 8) + "px";
      dock.style.top = clamp(rect.top, 8, window.innerHeight - rect.height - 8) + "px";
    }
    positionPanel();
  });

})();
